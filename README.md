Para iniciar os testes primeiro utilize o comando: `npm install --save-dev jest`

Isso irá instalar todos os módulos necessários para rodar os testes.

Depois utilize o comando `npm test` para realizar os testes.
